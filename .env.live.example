# Live environment example (no secrets)
# Copy to .env.live and fill values locally. Do not commit actual .env.live.
# This file is for presence-only secrets used by the worker in live mode.
# The backend passes these through to the worker container.
# Never echo secrets in logs or scripts.

# Required for live Codex worker
OPENAI_API_KEY=
OPENAI_ORG_ID=
OPENAI_PROJECT=

# Optional: leave unset if not used
# PROXY_URL=
# WORKER_EXTRA_FLAGS=