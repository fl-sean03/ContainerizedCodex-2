[pytest]
# Limit test discovery to backend tests to avoid collecting generated workspace samples
testpaths = backend/tests
# Keep default verbosity consistent
addopts = -q
# Ensure "backend" is on sys.path for imports like "from backend.main import create_app"
pythonpath = backend